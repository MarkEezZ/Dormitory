#include "SortService.h"

list<Student> quickSort(list<Student> students)
{
    return list<Student>();
}

list<Student> selectionSort(list<Student> students)
{
    return list<Student>();
}

list<Student> timsort(list<Student> students)
{
    return list<Student>();
}
